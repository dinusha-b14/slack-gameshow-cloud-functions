'use strict';

/**
 * @module messages
 * @description An index of all messages used within the app.
 */

const welcomeMessage = require('./welcomeMessage');

module.exports = {
    welcomeMessage
};
